package org.acme;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativeParentChildResourceIT extends ParentChildResourceTest {

    // Execute the same tests but in native mode.
}